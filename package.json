{
  "name": "genshin-guia",
  "version": "1.1.0",

  "scripts": {
    "test": "jest"
  },

  "devDependencies": {
    "jest": "^29.7.0",
    "jest-fetch-mock": "^3.0.3",
    "jest-environment-jsdom": "^29.7.0"
  },

  "jest": {
    "testEnvironment": "jsdom",

    "setupFiles": [
      "./jest.setup.js"
    ]
  }
}
